<UserControl x:Class="Music.Applications.Windows.Components.NoItemsComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <StackPanel Orientation="Horizontal">
        <Viewbox Width="100px" Height="100px">
            <Path Data="{StaticResource ItemsNotFoundIcon}"
                  Fill="{DynamicResource AccentTextColor}" />
        </Viewbox>
        <TextBlock Text="{DynamicResource NoItemsFoundLabel}"
                   HorizontalAlignment="Center" Style="{StaticResource HeaderText}"
                   VerticalAlignment="Center"
                   FontSize="20" />
    </StackPanel>
</UserControl>